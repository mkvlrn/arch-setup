{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "bun",
      "request": "launch",
      "name": "Debug current Bun test",
      "runtime": "${env:HOME}/.local/bin/bunc",
      "program": "${workspaceFolder}",
      "args": ["test", "${file}"],
      "cwd": "${workspaceFolder}",
      "stopOnEntry": false
    }
  ]
}
