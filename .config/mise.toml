[tools]
biome = "2.5.8"
bun = "1.3.14"
cocogitto = "7.0.0"
lefthook = "2.1.10"

[hooks]
postinstall = "lefthook install"

[env]
_.path = ["./node_modules/.bin"]

[settings]
disable_backends = ["asdf"]
use_versions_host = false
minimum_release_age = "1d"

[tasks.dev]
description = "Dev mode"
run = "bunc main.ts"
[tasks.lint]
description = "Check files with biome"
run = "biome check --diagnostic-level=warn --no-errors-on-unmatched"
[tasks.lint-fix]
description = "Fix files with biome"
run = "biome check --diagnostic-level=warn --no-errors-on-unmatched --write"
[tasks.typecheck]
description = "Run tsc"
run = "tsc"
[tasks.test]
description = "Run tests"
run = "bunc test --coverage --pass-with-no-tests"
[tasks.test-staged]
description = "Run tests on staged files"
run = "bunc test --changed --bail --reporter=dots --pass-with-no-tests"
[tasks.test-ci]
description = "Run tests in ci mode"
run = "bunc test --bail --reporter=dots --pass-with-no-tests"
