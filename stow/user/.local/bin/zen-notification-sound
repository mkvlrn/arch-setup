#!/usr/bin/env bash

dbus-monitor "interface='org.freedesktop.Notifications',member='Notify'" | while read -r line; do
  if [[ "$line" =~ variant.*\"zen\" ]]; then
    paplay /usr/share/sounds/freedesktop/stereo/message-new-instant.oga >/dev/null 2>&1 &
  fi
done
